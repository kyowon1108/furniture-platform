# 데이터 흐름 상세 가이드

## 📋 목차
1. [전체 아키텍처](#전체-아키텍처)
2. [인증 흐름](#인증-흐름)
3. [프로젝트 생성 흐름](#프로젝트-생성-흐름)
4. [가구 추가 흐름](#가구-추가-흐름)
5. [실시간 동기화 흐름](#실시간-동기화-흐름)
6. [저장 흐름](#저장-흐름)
7. [Undo/Redo 흐름](#undoredo-흐름)

---

## 전체 아키텍처

```
┌─────────────┐
│   Browser   │
│  (React)    │
└──────┬──────┘
       │
       ├─── HTTP/REST ───┐
       │                 │
       └─── WebSocket ───┤
                         │
                    ┌────▼────┐
                    │ FastAPI │
                    │ Server  │
                    └────┬────┘
                         │
                    ┌────▼────┐
                    │ SQLite  │
                    │Database │
                    └─────────┘
```

---

## 인증 흐름

### 1. 회원가입

```
사용자 입력 (이메일, 비밀번호)
    ↓
authStore.register()
    ↓
POST /api/v1/auth/register
    ↓
백엔드: 이메일 중복 확인
    ↓
백엔드: 비밀번호 해싱 (bcrypt)
    ↓
백엔드: User 생성 및 DB 저장
    ↓
응답: User 객체 (비밀번호 제외)
    ↓
자동 로그인
```

### 2. 로그인

```
사용자 입력 (이메일, 비밀번호)
    ↓
authStore.login()
    ↓
POST /api/v1/auth/login (OAuth2PasswordRequestForm)
    ↓
백엔드: 사용자 조회
    ↓
백엔드: 비밀번호 검증
    ↓
백엔드: JWT 토큰 생성
    ↓
응답: { access_token, token_type }
    ↓
localStorage에 토큰 저장
    ↓
authStore.fetchUser() 호출
    ↓
GET /api/v1/auth/me (Authorization: Bearer {token})
    ↓
응답: User 객체
    ↓
authStore 업데이트 (user, isAuthenticated)
```

### 3. 인증된 요청

```
API 요청
    ↓
axios interceptor: localStorage에서 토큰 가져오기
    ↓
요청 헤더에 Authorization: Bearer {token} 추가
    ↓
백엔드: JWT 토큰 검증
    ↓
백엔드: 토큰에서 사용자 이메일 추출
    ↓
백엔드: DB에서 사용자 조회
    ↓
백엔드: 요청 처리
    ↓
응답 반환
```

---

## 프로젝트 생성 흐름

### 1. 수동 입력 방식 (기본)

```
사용자: "새 프로젝트" 버튼 클릭
    ↓
CreateProjectModal 표시
    ↓
사용자: "수동 입력" 모드 선택 (기본값)
    ↓
사용자: 프로젝트 정보 입력
  - 이름
  - 방 크기 (폭, 높이, 깊이)
  - 설명 (선택)
    ↓
"생성" 버튼 클릭
    ↓
projectsAPI.create(data)
    ↓
POST /api/v1/projects
  Headers: Authorization: Bearer {token}
  Body: { name, room_width, room_height, room_depth, description, has_ply_file: false }
    ↓
백엔드: JWT 토큰 검증 → 현재 사용자 확인
    ↓
백엔드: Project 생성
  - owner_id = current_user.id
  - 입력 데이터로 필드 설정
    ↓
백엔드: DB에 Project 저장
    ↓
백엔드: 초기 빈 레이아웃 생성
  - Layout(project_id, version=1, furniture_state={"furnitures": []}, is_current=True)
    ↓
백엔드: DB에 Layout 저장
    ↓
응답: Project 객체
    ↓
프론트엔드: router.push(`/editor/${project.id}`)
    ↓
에디터 페이지로 자동 이동
```

### 2. PLY 파일 업로드 방식

```
사용자: "새 프로젝트" 버튼 클릭
    ↓
CreateProjectModal 표시
    ↓
사용자: "PLY 파일 업로드" 모드 선택
    ↓
사용자: 프로젝트 정보 입력
  - 이름
  - PLY 파일 선택 (.ply, 크기 제한 없음)
  - 방 크기 (폭, 높이, 깊이)
  - 설명 (선택)
    ↓
"생성" 버튼 클릭
    ↓
Step 1: 프로젝트 생성
  projectsAPI.create({ ...data, has_ply_file: true })
    ↓
POST /api/v1/projects
  Body: { name, room_width, room_height, room_depth, description, has_ply_file: true }
    ↓
백엔드: Project 생성 및 저장
    ↓
응답: Project 객체 (project_id 포함)
    ↓
Step 2: PLY 파일 업로드
  filesAPI.uploadPly(project_id, plyFile)
    ↓
POST /api/v1/files/upload-ply/{project_id}
  Headers: Authorization: Bearer {token}
  Content-Type: multipart/form-data
  Body: FormData with file
    ↓
백엔드: 파일 검증
  - 확장자 확인 (.ply)
  - PLY 형식 검증 (plyfile 라이브러리)
  - Vertex 데이터 존재 확인
  - 참고: 개발/시연용으로 크기 제한 없음
    ↓
백엔드: 파일 저장
  - 경로: uploads/ply_files/project_{id}_{filename}.ply
    ↓
백엔드: Project 업데이트
  - has_ply_file = True
  - ply_file_path = 저장 경로
  - ply_file_size = 파일 크기
    ↓
응답: 업로드 결과 (vertex_count 포함)
    ↓
프론트엔드: router.push(`/editor/${project.id}`)
    ↓
에디터 페이지로 자동 이동
    ↓
3D 씬에 PLY 모델 렌더링
```

---

## 가구 추가 흐름

### 1. 사이드바에서 클릭

```
사용자: 사이드바에서 가구 클릭
    ↓
Sidebar.handleClick(catalogItem)
    ↓
새 FurnitureItem 생성
  - id: `${catalogItem.id}-${Date.now()}`
  - type, dimensions, color: catalogItem에서 복사
  - position: { x: 0, y: height/2, z: 0 }
  - rotation, scale: 기본값
    ↓
editorStore.addFurniture(newFurniture)
    ↓
editorStore 상태 업데이트
  - furnitures: [...furnitures, newFurniture]
  - hasUnsavedChanges: true
    ↓
editorStore.saveToHistory() 호출
  - historyStack에 현재 상태 저장
    ↓
socketService.emitFurnitureAdd(newFurniture)
    ↓
WebSocket: emit 'furniture_add'
  Data: { project_id, furniture }
    ↓
백엔드: 같은 프로젝트의 다른 사용자에게 브로드캐스트
    ↓
다른 클라이언트: 'furniture_added' 이벤트 수신
    ↓
다른 클라이언트: editorStore.addFurniture(furniture)
    ↓
모든 클라이언트: 3D 씬에 가구 렌더링
```

### 2. 드래그 앤 드롭

```
사용자: 사이드바에서 가구 드래그 시작
    ↓
Sidebar.handleDragStart(e, catalogItem)
    ↓
e.dataTransfer.setData('furniture', JSON.stringify(catalogItem))
    ↓
사용자: 3D 씬 위로 드래그
    ↓
Scene.handleDragOver(e)
  - e.preventDefault()
  - e.dataTransfer.dropEffect = 'copy'
    ↓
사용자: 3D 씬에 드롭
    ↓
Scene.handleDrop(e)
    ↓
e.dataTransfer.getData('furniture') 파싱
    ↓
새 FurnitureItem 생성
    ↓
editorStore.addFurniture(newFurniture)
    ↓
(이후 클릭과 동일한 흐름)
```

---

## 실시간 동기화 흐름

### 1. 연결 설정

```
에디터 페이지 로드
    ↓
useSocket(projectId, userId) 호출
    ↓
socketService.connect(projectId, userId)
    ↓
Socket.IO 연결 생성
  URL: http://localhost:8000
  Transport: websocket
    ↓
백엔드: 'connect' 이벤트 수신
    ↓
프론트엔드: 'connect' 이벤트 수신
    ↓
프론트엔드: emit 'join_project'
  Data: { project_id, user_id }
    ↓
백엔드: 사용자를 프로젝트 룸에 추가
  - sio.enter_room(sid, f'project_{project_id}')
  - active_rooms[project_id].add(sid)
    ↓
백엔드: 같은 룸의 다른 사용자에게 브로드캐스트
  - emit 'user_joined' (skip_sid=sid)
    ↓
다른 클라이언트: 'user_joined' 이벤트 수신
    ↓
Toast 알림: "User {user_id} joined"
```

### 2. 가구 이동 동기화

```
사용자 A: 가구 드래그 (미래 기능)
    ↓
editorStore.updateFurniture(id, { position, rotation })
    ↓
socketService.emitFurnitureMove(id, position, rotation)
    ↓
WebSocket: emit 'furniture_move'
  Data: { project_id, furniture_id, position, rotation }
    ↓
백엔드: 'furniture_move' 이벤트 수신
    ↓
백엔드: 같은 룸의 다른 사용자에게 브로드캐스트
  - emit 'furniture_updated' (skip_sid=sender_sid)
    ↓
사용자 B, C: 'furniture_updated' 이벤트 수신
    ↓
editorStore.updateFurniture(furniture_id, { position, rotation })
    ↓
3D 씬 자동 업데이트 (React 리렌더링)
```

---

## 저장 흐름

### 1. 자동 저장

```
useAutoSave(5000) 훅 실행
    ↓
5초마다 setInterval 콜백 실행
    ↓
hasUnsavedChanges 확인
    ↓
true인 경우:
    ↓
editorStore.saveLayout() 호출
    ↓
(이후 수동 저장과 동일)
```

### 2. 수동 저장 (Ctrl+S 또는 버튼 클릭)

```
사용자: Ctrl+S 또는 "저장" 버튼 클릭
    ↓
editorStore.saveLayout() 호출
    ↓
상태 확인:
  - projectId 존재?
  - isSaving = false?
    ↓
set({ isSaving: true })
    ↓
furniture_state = { furnitures: get().furnitures }
    ↓
layoutsAPI.save(projectId, furniture_state)
    ↓
POST /api/v1/projects/{projectId}/layouts
  Headers: Authorization: Bearer {token}
  Body: { furniture_state }
    ↓
백엔드: JWT 토큰 검증 → 현재 사용자 확인
    ↓
백엔드: 프로젝트 소유자 확인
    ↓
백엔드: 모든 기존 레이아웃의 is_current = False 업데이트
    ↓
백엔드: 최대 version 번호 조회
    ↓
백엔드: 새 Layout 생성
  - project_id
  - version = max_version + 1
  - furniture_state (JSON)
  - is_current = True
    ↓
백엔드: DB에 Layout 저장
    ↓
응답: Layout 객체
    ↓
프론트엔드: 상태 업데이트
  - lastSaved = new Date()
  - hasUnsavedChanges = false
  - isSaving = false
    ↓
Toast 알림: "✓ 레이아웃 저장 완료"
```

---

## Undo/Redo 흐름

### 1. History 스택 관리

```
가구 추가/수정/삭제 액션
    ↓
editorStore.saveToHistory() 호출
    ↓
현재 상태 스냅샷 생성
  currentState = { furnitures: [...furnitures] }
    ↓
redo 히스토리 제거
  newStack = historyStack.slice(0, historyIndex + 1)
    ↓
새 상태 추가
  newStack.push(currentState)
    ↓
크기 제한 확인 (maxHistorySize = 30)
  if (newStack.length > 30) newStack.shift()
    ↓
상태 업데이트
  - historyStack = newStack
  - historyIndex = newStack.length - 1
  - canUndo = true
  - canRedo = false
```

### 2. Undo (실행 취소)

```
사용자: Ctrl+Z 또는 "↶" 버튼 클릭
    ↓
editorStore.undo() 호출
    ↓
canUndo 확인 (historyIndex > 0)
    ↓
newIndex = historyIndex - 1
    ↓
previousState = historyStack[newIndex]
    ↓
상태 업데이트
  - furnitures = [...previousState.furnitures]
  - historyIndex = newIndex
  - hasUnsavedChanges = true
  - canUndo = newIndex > 0
  - canRedo = true
    ↓
3D 씬 자동 업데이트
    ↓
Toast 알림: "↶ 실행 취소"
```

### 3. Redo (다시 실행)

```
사용자: Ctrl+Y 또는 "↷" 버튼 클릭
    ↓
editorStore.redo() 호출
    ↓
canRedo 확인 (historyIndex < historyStack.length - 1)
    ↓
newIndex = historyIndex + 1
    ↓
nextState = historyStack[newIndex]
    ↓
상태 업데이트
  - furnitures = [...nextState.furnitures]
  - historyIndex = newIndex
  - hasUnsavedChanges = true
  - canUndo = true
  - canRedo = newIndex < historyStack.length - 1
    ↓
3D 씬 자동 업데이트
    ↓
Toast 알림: "↷ 다시 실행"
```

---

## 측정 도구 흐름

```
사용자: "📏 거리 측정" 버튼 클릭
    ↓
editorStore.setMeasureMode('distance')
    ↓
상태 업데이트
  - measureMode = 'distance'
  - measurePoints = []
    ↓
사용자: 3D 씬의 바닥 클릭 (첫 번째 포인트)
    ↓
Scene.handleCanvasClick(e)
    ↓
Raycaster로 바닥 평면과의 교차점 계산
  - plane = new THREE.Plane(new Vector3(0, 1, 0), 0)
  - raycaster.ray.intersectPlane(plane, intersectPoint)
    ↓
editorStore.addMeasurePoint({ x, y: 0, z })
    ↓
measurePoints에 포인트 추가
    ↓
3D 씬에 빨간 구체 렌더링
    ↓
사용자: 바닥 클릭 (두 번째 포인트)
    ↓
(동일한 과정)
    ↓
measurePoints.length === 2
    ↓
거리 계산
  distance = Math.sqrt((p2.x - p1.x)² + (p2.z - p1.z)²)
    ↓
3D 씬에 녹색 라인 렌더링
    ↓
MeasurePanel에 거리 표시: "거리: X.XXm"
```

---

## 조명 시뮬레이션 흐름

```
사용자: LightingPanel에서 시간대 선택 (예: "저녁 🌇")
    ↓
editorStore.setTimeOfDay('evening')
    ↓
상태 업데이트
  - timeOfDay = 'evening'
    ↓
Scene 컴포넌트 리렌더링
    ↓
lightingConfig[timeOfDay] 조회
  - position: [-10, 5, 5]
  - intensity: 0.8
  - color: '#FFB347'
  - ambient: 0.3
    ↓
Three.js 조명 업데이트
  - ambientLight.intensity = 0.3
  - directionalLight.position = [-10, 5, 5]
  - directionalLight.intensity = 0.8
  - directionalLight.color = '#FFB347'
    ↓
3D 씬 자동 재렌더링
    ↓
그림자 및 조명 효과 업데이트
```

---

## 복사/붙여넣기 흐름

### 1. 복사 (Ctrl+C)

```
사용자: 가구 선택 후 Ctrl+C
    ↓
editorStore.copySelected() 호출
    ↓
선택된 가구 필터링
  selected = furnitures.filter(f => selectedIds.includes(f.id))
    ↓
clipboard에 저장
  set({ clipboard: selected })
    ↓
Toast 알림: "📋 X개 복사됨"
```

### 2. 붙여넣기 (Ctrl+V)

```
사용자: Ctrl+V
    ↓
editorStore.paste() 호출
    ↓
clipboard 확인
  if (clipboard.length === 0) return
    ↓
clipboard의 각 아이템에 대해:
    ↓
새 FurnitureItem 생성
  - id: 새 고유 ID
  - position: 원본 + offset (0.5m)
  - 나머지: 원본 복사
    ↓
editorStore.addFurniture(newItem)
    ↓
socketService.emitFurnitureAdd(newItem)
    ↓
Toast 알림: "📋 X개 붙여넣기 완료"
    ↓
3D 씬에 복사본 렌더링
```

---

## 다음 단계

- [API 문서](./05_API_문서.md) 상세 참조
- [개발 가이드](./06_개발_가이드.md)로 시작하기
